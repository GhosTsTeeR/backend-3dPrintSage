const express = require('express');
const router = express.Router();
const printsController = require('../../controllers/app_3dprintsage/printsController');

// Agrega las rutas relacionadas con las impresoras 3D aquí

module.exports = router;